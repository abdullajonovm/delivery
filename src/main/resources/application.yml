#spring.datasource.url=jdbc:postgresql://localhost:5432/andqxai
server:
  port: 6100
spring:
  datasource:
    url: jdbc:postgresql://postgres:axjDr1TSnwyrqOuAg4F7@containers-us-west-128.railway.app:6100/railway
    username: postgres
    password: axjDr1TSnwyrqOuAg4F7
    driver-class-name: org.postgresql.Driver
  profiles:
    active: #activeProfile#
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: create
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
#  config:
#    activate:
#      on-profile: spring.profiles.active@
springdoc:
  swagger-ui:
    path: /swagger-ui
    enabled: true
  api-docs:
    path: /api-docs
